{
  "mcpServers": {
    "hermes": {
      "command": "uvx",
      "args": ["hermes-agent-mcp"],
      "env": {
        "HERMES_MCP_ALLOW_WRITE": "1",
        "HERMES_MCP_ALLOW_SHELL": "0"
      }
    }
  }
}
